# Statistics_Predict
Telegram bot + Python script + Xgboost Model

```
          ┌───────────────┐
          │ Telegram 用户 │
          │ 发送命令     │
          └─────┬─────────┘
                │
                ▼
        ┌─────────────────┐
        │ Telegram Bot     │
        │ (python-telegram-bot) │
        └─────┬─────────┘
              │ 解析指令
              ▼
        ┌─────────────────┐
        │ 调用 Python 脚本 │
        │ (抓取数据 & 特征)│
        └─────┬─────────┘
              │
              ▼
┌───────────────────────────────────┐
│ 数据抓取模块                       │
│ - 股票日线 OHLCV + 复权因子        │
│ - 财报 / 基本面数据                 │
│ - 行业 / 季节 / Fama-French 因子   │
│ - 新闻 / 情绪（可选）               │
└─────┬─────────────────────────────┘
      │
      ▼
┌──────────────────────────────┐
│ 特征工程模块                  │
│ - 滚动收益率 / 波动率 / 均线   │
│ - 行业/季节/月份 one-hot 编码 │
│ - 横截面标准化 / 分位数 z-score│
│ - 残差因子处理（可选）         │
└─────┬───────────────────────┘
      │
      ▼
┌──────────────────────────────┐
│ 本地 XGBoost 模型             │
│ - 用历史数据训练               │
│ - 接收最新特征表预测未来收益率 │
│ - 输出每只股票预测值或排名     │
└─────┬───────────────────────┘
      │
      ▼
┌──────────────────────────────┐
│ Telegram Bot 返回结果         │
│ - 排序表格 / CSV / 图表       │
│ - 可选通知每日策略调仓        │
└──────────────────────────────┘

```
